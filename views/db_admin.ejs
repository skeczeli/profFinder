<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Gestión Académica</title>
    <!-- Manteniendo tus estilos ya que la estructura depende de ellos -->
    <style>
      body {
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
        background-color: #f5f7fa;
        display: flex;
        flex-direction: column;
        justify-content: flex-start;
        align-items: center;
        min-height: 100vh;
        margin: 0;
        padding: 20px;
      }

      h1 {
        font-size: 2.5rem;
        color: #1a3b5d;
        margin-top: 40px;
        margin-bottom: 30px;
        font-weight: 600;
      }

      h2,
      h3 {
        color: #1a3b5d;
        font-weight: 600;
        margin-top: 0;
        margin-bottom: 24px;
        font-size: 1.8rem;
      }

      .container {
        width: 100%;
        max-width: 1100px;
        display: flex;
        flex-direction: column;
        align-items: center;
        gap: 20px;
      }

      .form-container {
        background-color: white;
        border-radius: 12px;
        box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
        padding: 32px;
        width: 100%;
        max-width: 450px;
        transition: all 0.3s ease;
        margin-bottom: 20px;
      }

      .form-container:hover {
        box-shadow: 0 12px 32px rgba(0, 0, 0, 0.15);
      }

      .input-group {
        margin-bottom: 24px;
      }

      label {
        display: block;
        font-size: 1rem;
        color: #4a5568;
        margin-bottom: 10px;
        font-weight: 500;
      }

      input,
      select {
        width: 100%;
        padding: 14px 18px;
        border: 1px solid #e2e8f0;
        border-radius: 8px;
        font-size: 1rem;
        transition: all 0.2s;
        background-color: #f9fafc;
        box-sizing: border-box;
      }

      input:focus,
      select:focus {
        outline: none;
        border-color: #4299e1;
        box-shadow: 0 0 0 3px rgba(66, 153, 225, 0.15);
        background-color: white;
      }

      button {
        background-color: #4d8ad5;
        color: white;
        border: none;
        border-radius: 8px;
        padding: 14px 24px;
        font-size: 1rem;
        font-weight: 600;
        cursor: pointer;
        transition: all 0.2s;
        width: 100%;
        margin-top: 12px;
      }

      button:hover {
        background-color: #3a75c4;
        transform: translateY(-1px);
      }

      button:active {
        transform: translateY(1px);
      }

      .btn-eliminar {
        background-color: #e53e3e;
      }

      .btn-eliminar:hover {
        background-color: #c53030;
        transform: translateY(-1px);
      }

      .table-container {
        background-color: white;
        border-radius: 12px;
        box-shadow: 0 8px 24px rgba(0, 0, 0, 0.1);
        padding: 32px;
        width: 100%;
        max-width: 900px;
        transition: all 0.3s ease;
        margin-top: 30px;
        overflow-x: auto;
      }

      table {
        width: 100%;
        border-collapse: collapse;
      }

      th,
      td {
        padding: 12px 15px;
        text-align: left;
        border-bottom: 1px solid #e2e8f0;
        white-space: nowrap;
      }

      th {
        background-color: #f8fafc;
        color: #1a3b5d;
        font-weight: 600;
      }

      tr:hover {
        background-color: #f9fafc;
      }

      .collapsible {
        cursor: pointer;
        user-select: none;
        padding: 12px 20px;
        background-color: #4d8ad5;
        color: white;
        border: none;
        border-radius: 8px;
        text-align: center;
        font-weight: 600;
        transition: background-color 0.2s;
        width: auto;
        margin-right: 10px;
        margin-bottom: 10px;
      }

      .collapsible:hover {
        background-color: #3a75c4;
      }

      .tab-content {
        display: none;
        width: 100%;
        flex-direction: column;
        align-items: center;
        gap: 20px;
      }

      .tab-buttons {
        margin-bottom: 30px;
        display: flex;
        flex-wrap: wrap;
        justify-content: center;
      }

      .active {
        display: flex;
      }

      @media (max-width: 800px) {
        .form-container {
          max-width: 100%;
        }
        .table-container {
          max-width: 100%;
        }
      }

      .home-button {
        position: absolute;
        top: 20px;
        left: 20px;
        display: inline-block;
        padding: 12px 24px;
        background-color: #4d8ad5;
        color: white;
        border-radius: 8px;
        transition: all 0.2s;
        text-decoration: none;
        font-weight: 600;
        z-index: 1000;
      }

      .home-button:hover {
        background-color: #3a75c4;
        transform: translateY(-2px);
      }

      .logout-button {
        position: absolute;
        top: 20px;
        right: 20px;
      }
      .logout-button button {
        padding: 12px 24px;
        width: auto;
        margin-top: 0;
      }
      .error-message {
        color: #e53e3e;
        font-weight: bold;
        text-align: center;
        margin-bottom: 20px;
        width: 100%;
      }

      .btn-editar {
        background-color: #4caf50;
        color: white;
        padding: 5px 10px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        text-decoration: none;
        font-size: 0.9em;
        margin-right: 5px;
        display: inline-block;
      }

      .btn-editar:hover {
        background-color: #45a049;
      }

      .btn-eliminar {
        background-color: #f44336;
        color: white;
        padding: 5px 10px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        font-size: 0.9em;
      }

      .btn-eliminar:hover {
        background-color: #d32f2f;
      }
    </style>
  </head>
  <body>
    <a href="/" class="home-button">Home</a>

    <form action="/admin/logout" method="POST" class="logout-button">
      <button type="submit" style="background-color: #e53e3e">
        Cerrar sesión
      </button>
    </form>

    <h1>Gestión Académica</h1>

    <!-- Mostrar mensaje de error si vino del backend -->
    <% if (typeof error !== 'undefined' && error) { %>
    <p class="error-message"><%= error %></p>
    <% } %>

    <div class="tab-buttons">
      <button class="collapsible" onclick="openTab('profesores')">
        Profesores
      </button>
      <button class="collapsible" onclick="openTab('aulas')">
        <!-- AÑADIDO BOTÓN AULAS -->
        Aulas
      </button>
      <button class="collapsible" onclick="openTab('materias')">
        Materias
      </button>
      <button class="collapsible" onclick="openTab('asignaciones')">
        Asignaciones
      </button>
    </div>

    <div class="container">
      <!-- Pestaña de Profesores -->
      <div id="profesores" class="tab-content">
        <div class="form-container">
          <h2>Registrar Profesor</h2>
          <div class="input-group">
            <label for="prof-nombre">Nombre completo:</label>
            <input
              type="text"
              id="prof-nombre"
              placeholder="Nombre y apellido"
            />
          </div>
          <div class="input-group">
            <label for="prof-email">Email:</label>
            <input
              type="email"
              id="prof-email"
              placeholder="correo@ejemplo.com"
            />
          </div>
          <div class="input-group">
            <label for="prof-tarjeta">ID de tarjeta:</label>
            <input type="text" id="prof-tarjeta" placeholder="Identificación" />
          </div>
          <div style="display: flex; gap: 10px">
            <button id="guardar-profesor">Guardar Profesor</button>
            <button id="borrar-profesor" class="btn-eliminar">
              Eliminar Profesor
            </button>
          </div>
          <p style="font-size: 0.8em; margin-top: 15px; color: #666">
            (Funcionalidad de guardar/borrar requiere implementación adicional)
          </p>
        </div>
        <div class="table-container">
          <h2>Listado de Profesores</h2>
          <table id="tabla-profesores">
            <thead>
              <tr>
                <th>Nombre</th>
                <th>Email</th>
                <th>ID de Tarjeta</th>
                <th>ID Profesor</th>
                <th>Acciones</th>
                <!-- Nueva columna para acciones -->
              </tr>
            </thead>
            <tbody id="tbody-profesores">
              <% if (profesores && profesores.length > 0) { %> <%
              profesores.forEach(profesor => { %>
              <tr>
                <td><%= profesor.nombre %></td>
                <td><%= profesor.email ? profesor.email : '-' %></td>
                <td><%= profesor.tarjeta_id %></td>
                <td><%= profesor.profesor_id %></td>
                <td>
                  <!-- Botón de edición -->
                  <a
                    href="/admin/profesores/editar/<%= profesor.profesor_id %>"
                    class="btn-editar"
                    >Editar</a
                  >
                  <!-- Puedes agregar también un botón de eliminar conectado a la API -->
                  <button
                    class="btn-eliminar"
                    data-tarjeta="<%= profesor.tarjeta_id %>"
                  >
                    Eliminar
                  </button>
                </td>
              </tr>
              <% }); %> <% } else { %>
              <tr>
                <td colspan="5">No hay profesores registrados.</td>
                <!-- Actualizado de 4 a 5 columnas -->
              </tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>

      <!-- Pestaña de Aulas -->
      <div id="aulas" class="tab-content">
        <!-- Puedes añadir un form para aulas aquí si lo necesitas -->
        <div class="form-container">
          <h2>Registrar Aula</h2>
          <div class="input-group">
            <label for="aula-nombre">Nombre del Aula:</label>
            <input
              type="text"
              id="aula-nombre"
              placeholder="Ej: Laboratorio 1"
            />
          </div>
          <div class="input-group">
            <label for="aula-esp32">ID ESP32 (Opcional):</label>
            <input
              type="text"
              id="aula-esp32"
              placeholder="ID del dispositivo asociado"
            />
          </div>
          <div style="display: flex; gap: 10px">
            <button id="guardar-aula">Guardar Aula</button>
            <button id="borrar-aula" class="btn-eliminar">Eliminar Aula</button>
          </div>
          <p style="font-size: 0.8em; margin-top: 15px; color: #666">
            (Funcionalidad requiere implementación adicional)
          </p>
        </div>

        <div class="table-container">
          <h2>Listado de Aulas</h2>
          <table id="tabla-aulas">
            <thead>
              <tr>
                <th>Nombre</th>
                <th>ESP32 ID</th>
                <th>ID Aula</th>
              </tr>
            </thead>
            <tbody id="tbody-aulas">
              <% if (aulas && aulas.length > 0) { %> <% aulas.forEach(aula => {
              %>
              <tr>
                <td><%= aula.nombre %></td>
                <td><%= aula.esp32_id ? aula.esp32_id : '-' %></td>
                <td><%= aula.aula_id %></td>
              </tr>
              <% }); %> <% } else { %>
              <tr>
                <td colspan="3">No hay aulas registradas.</td>
              </tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>

      <!-- Pestaña de Materias -->
      <div id="materias" class="tab-content">
        <div class="form-container">
          <h2>Registrar Materia</h2>
          <div class="input-group">
            <label for="materia-nombre">Nombre de la materia:</label>
            <input
              type="text"
              id="materia-nombre"
              placeholder="Ej: Cálculo I"
            />
          </div>
          <div style="display: flex; gap: 10px">
            <button id="guardar-materia">Guardar Materia</button>
            <button id="borrar-materia" class="btn-eliminar">
              Eliminar Materia
            </button>
          </div>
          <p style="font-size: 0.8em; margin-top: 15px; color: #666">
            (Funcionalidad requiere implementación adicional)
          </p>
        </div>

        <div class="table-container">
          <h2>Listado de Materias</h2>
          <table id="tabla-materias">
            <thead>
              <tr>
                <th>Nombre</th>
                <th>ID Materia</th>
              </tr>
            </thead>
            <tbody id="tbody-materias">
              <% if (materias && materias.length > 0) { %> <%
              materias.forEach(materia => { %>
              <tr>
                <td><%= materia.nombre %></td>
                <td><%= materia.materia_id %></td>
              </tr>
              <% }); %> <% } else { %>
              <tr>
                <td colspan="2">No hay materias registradas.</td>
              </tr>
              <% } %>
            </tbody>
          </table>
        </div>
      </div>

      <!-- Pestaña de Asignaciones -->
      <div id="asignaciones" class="tab-content">
        <div class="form-container">
          <h2>Asignar Materias a Profesores</h2>
          <div class="input-group">
            <label for="asign-profesor">Profesor:</label>
            <select id="asign-profesor">
              <option value="">Seleccione un profesor</option>
              <% if (profesores && profesores.length > 0) { %> <%
              profesores.forEach(profesor => { %>
              <option value="<%= profesor.profesor_id %>">
                <%= profesor.nombre %> (ID: <%= profesor.profesor_id %>)
              </option>
              <% }); %> <% } %>
            </select>
          </div>
          <div class="input-group">
            <label for="asign-materia">Materia:</label>
            <select id="asign-materia">
              <option value="">Seleccione una materia</option>
              <% if (materias && materias.length > 0) { %> <%
              materias.forEach(materia => { %>
              <option value="<%= materia.materia_id %>">
                <%= materia.nombre %> (ID: <%= materia.materia_id %>)
              </option>
              <% }); %> <% } %>
            </select>
          </div>
          <div style="display: flex; gap: 10px">
            <button id="guardar-asignacion">Guardar Asignación</button>
            <button id="borrar-asignacion" class="btn-eliminar">
              Eliminar Asignación
            </button>
          </div>
          <p style="font-size: 0.8em; margin-top: 15px; color: #666">
            (Funcionalidad requiere implementación adicional)
          </p>
        </div>

        <div class="table-container">
          <h2>Asignaciones Actuales</h2>
          <table id="tabla-asignaciones">
            <thead>
              <tr>
                <th>Profesor (ID)</th>
                <th>Materia (ID)</th>
              </tr>
            </thead>
            <tbody id="tbody-asignaciones">
              <tr>
                <td colspan="2">
                  Datos no cargados. Se requiere actualizar backend y/o usar
                  JS/AJAX.
                </td>
              </tr>
              <!--
              Ejemplo si tuvieras un array 'asignacionesData' con {prof_nombre, mat_nombre}:
              <% if (typeof asignacionesData !== 'undefined' && asignacionesData.length > 0) { %>
                  <% asignacionesData.forEach(asig => { %>
                      <tr>
                        <td><%= asig.prof_nombre %></td>
                        <td><%= asig.mat_nombre %></td>
                      </tr>
                  <% }); %>
              <% } else { %>
                      <tr>
                        <td colspan="2">No hay asignaciones registradas.</td>
                      </tr>
              <% } %>
              -->
            </tbody>
          </table>
        </div>
      </div>
    </div>

    <script>
      function openTab(tabName) {
        const tabContents = document.getElementsByClassName("tab-content");
        for (let i = 0; i < tabContents.length; i++) {
          tabContents[i].style.display = "none";
          tabContents[i].classList.remove("active");
        }
        const activeTab = document.getElementById(tabName);
        if (activeTab) {
          activeTab.style.display = "flex";
          activeTab.classList.add("active");
        }
      }

      document.addEventListener("DOMContentLoaded", function () {
        openTab("profesores");
      });
    </script>
    <script src="/js/profesores.js"></script>
  </body>
</html>
