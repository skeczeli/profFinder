<!DOCTYPE html>
<html>
  <head>
    <title><%= profesorName %></title>
    <link rel="stylesheet" href="/styles.css" />
  </head>
  <body>
    <h1><%= profesorName %></h1>
    <h2>Ubicación actual: <%= profesorLocation %></h2>
    <div>
      <h2>Materias:</h2>
      <ul>
        <% profesor.materias.forEach((materia) => { %>
        <li>%= materia.nombre %></li>
        <% }); %>
      </ul>
    </div>
    <div>
      <h2>Contacta a tu profesor:</h2>
      <p>
        Para realizar una consulta breve, envía un mensaje a su aula. Tu
        profesor lo verá y podrá esperarte.
      </p>
      <form action="/profesor/<%= profesorId %>" method="POST">
        <input
          type="text"
          name="alumno"
          placeholder="Nombre del alumno"
          required
        />
        <input type="text" name="consulta" placeholder="Consulta" required />
        <button type="submit">Enviar</button>
      </form>
    </div>
  </body>
</html>
